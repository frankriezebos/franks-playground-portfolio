export function pageTransitions() {
    $('.page-wrapper').addClass('animateIn');
    $('.intro-wrapper').removeClass('hidden');
    $('.intro-wrapper').addClass('zoomIn');
    $('.logo p').addClass('loaded');
}